CODE SEGMENT
	ASSUME CS:CODE
START:
	;8255结IOY2口，初始化8255
	MOV DX,0686H
	MOV AL,90H
	OUT DX,AL
	
	;ad转换器接ioy1，选通0通道
AA1:	MOV DX,0640H
	OUT DX,AL
	CALL DELAY
	IN AL,DX
	MOV DX,0682H
	OUT DX,AL
	JMP AA1
DELAY:
	MOV CX,0FFFFH
L1:	
	LOOP L1
	RET 
	
CODE	ENDS
	END START