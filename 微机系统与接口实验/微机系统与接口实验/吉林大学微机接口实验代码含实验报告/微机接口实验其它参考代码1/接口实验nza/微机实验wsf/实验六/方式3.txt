CODE SEGMENT
	ASSUME CS:CODE
START:


;将8254 cs接IOY0，地址是0600H
L2:	MOV DX,0606H
	MOV AL,76H
	OUT DX,AL;选定计数器1工作在方式0,并且写入16位数字
	MOV DX,0602H
	MOV AL,0FFH;这是奇数：65535
	;MOV AL,0FEH 
	OUT DX,AL
	MOV AL,0FFH
	OUT DX,AL
	JMP L2
;将CLK1接在系统的CLK上

CODE ENDS
	END START