CODE SEGMENT
	ASSUME CS:CODE
START:



;将8254cs接IOY0，地址是0600H
L2:	MOV DX,0606H
	MOV AL,72H
	OUT DX,AL;选定计数器1工作在方式0,并且写入16位数字
	MOV DX,0602H
	MOV AL,0FFH
	OUT DX,AL
	MOV AL,0FFH
	OUT DX,AL
	CALL DELAY;计数完成，OUT1变为高电平保持？
	JMP L2
;开始时将GATE置于低电平，后快速置一后再置0形成一个gate输入脉冲，作为开始计数信号

DELAY:
	PUSH CX
	MOV CX,0FFFFH
L3:	
	LOOP L3
		MOV CX,0FFFFH
L4:	
	LOOP L4
		MOV CX,0FFFFH
L5:	
	LOOP L5
	POP CX
	RET
CODE ENDS
	END START