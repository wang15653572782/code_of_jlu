;疑问是主机怎么把从AD中读到的值在示波器上显示出来

CODE SEGMENT
	ASSUME CS:CODE
START:
	CLI
	MOV AL,0EFH
	OUT 21H,AL
	STI
	MOV CX,0AH
	;AD转换器接IOY3
	;DA转换器接IOY0
L1:	MOV AL,80H
	MOV DX,0600H
L2:	OUT DX,AL
	CALL DELAY
	INC AL
	INC AL
	INC AL
	INC AL
	INC AL
	INC AL	
	CMP AL,0FFH
	JBE L2
	LOOP L1 
	


DELAY:
	PUSH CX
	MOV CX,0FFFFH
L3:	
	LOOP L3
	POP CX
	RET
CODE ENDS
	END START
	

	
	