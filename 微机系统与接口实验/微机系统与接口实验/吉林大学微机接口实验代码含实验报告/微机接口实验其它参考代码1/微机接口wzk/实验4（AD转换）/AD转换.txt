CODE SEGMENT
	ASSUME CS:CODE
START:
	MOV AL,90H       ;8255
	MOV DX,0686H
	OUT DX,AL
LL3:	MOV AL,00H        
	MOV DX,0640H
	OUT DX,AL             ;做启动信号
	MOV CX,0FFH
	
LL1:    MOV BX,0FFH

LL2:    DEC BX       ;寄存器清零，V0输出为0
	JNZ LL2
	
	LOOP LL1
	MOV DX,0640H
	IN  AL,DX
	MOV DX,0683H
	OUT DX,AL
	JMP LL3	
	MOV AH,4CH
	INT 21H
CODE ENDS
END START
